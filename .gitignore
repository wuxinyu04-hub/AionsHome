# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# ── IDE ──
.vscode/
.idea/

# ── 用户数据（含隐私信息，整目录默认忽略；需要入库的用 ! 显式白名单） ──
# 之前是逐文件黑名单，每加一个新数据文件都可能被漏掉误提交，改为默认全忽略
aion-chat/data/**
!aion-chat/data/**/
!aion-chat/data/**/.gitkeep
!aion-chat/data/sleep_library.json

# ── Connor-Codex 用户数据 ──
Connor-Codex/messages.jsonl
Connor-Codex/persona.md
Connor-Codex/auto-responder-state.json
Connor-Codex/uploads/
Connor-Codex/node_modules/
Connor-Codex/package-lock.json
Connor-Codex/*.log

# ── 独立 Demo / 测试项目（不属于主程序） ──
Active/
LittleToy/
语音唤醒/
火山引擎TTSdemo/

# ── 个人文件（不应上传） ──
自己看的存档.txt
MW_RAG_Backup_*.json
import_mw_rag.py
fix_schedules.py

# ── Android 构建产物 ──
AionApp/app/build/
AionApp/.gradle/
AionApp/build/
AionApp/local.properties
*.apk
*.aab

# ── 其他 ──
*.mp3
!public/*.mp3
!public/**/*.mp3
!public/card/
!public/card/**
*.log
.DS_Store
Thumbs.db

# ── 头像默认备份（换头像时自动生成，每人不同） ──
public/UserIcon.default.png
public/AIIcon.default.png

# ── 本地安装包 / 安装器不入库 ──
*.exe
tailscale-setup.exe

# ── 启动日志（运行时生成，量大） ──
start.log
start_err.log

# ── 散落临时调试/生成脚本与大二进制（不入库） ──
aion-chat/_*.py
aion-chat/batch_*.py
aion-chat/upload_to_netease_cloud.py
_template_extracted.html
/*.zip
/温叙远*.html
/ChatGPT*.png
