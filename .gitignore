# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# ── IDE ──
.vscode/
.idea/

# ── 用户数据（含隐私信息，整目录默认忽略；需要入库的用 ! 显式白名单） ──
# 之前是逐文件黑名单，每加一个新数据文件都可能被漏掉误提交，改为默认全忽略
aion-chat/data/**

# ── mi-fitness 登录临时文件（二维码/登录链接都含一次性凭证，不入库） ──
public/mi_cloud_qr.png
aion-chat/data/mi_cloud_token.json
aion-chat/data/mi_cloud_login_url.txt

# ── Cloudflare Turnstile 一次性凭证（mi_cloud 登录用，JWT，绝不入库） ──
nvtoken.txt
!aion-chat/data/**/
!aion-chat/data/**/.gitkeep
!aion-chat/data/sleep_library.json

# ── Connor-Codex 用户数据 ──
Connor-Codex/messages.jsonl
Connor-Codex/persona.md
Connor-Codex/auto-responder-state.json
Connor-Codex/uploads/
Connor-Codex/node_modules/
Connor-Codex/package-lock.json
Connor-Codex/*.log
Connor-Codex/*.txt

# ── 独立 Demo / 测试项目（不属于主程序） ──
Active/
LittleToy/
语音唤醒/
火山引擎TTSdemo/

# ── 个人文件（不应上传） ──
自己看的存档.txt
MW_RAG_Backup_*.json
import_mw_rag.py
fix_schedules.py

# ── Android 构建产物 ──
AionApp/app/build/
AionApp/.gradle/
AionApp/build/
AionApp/local.properties
*.apk
*.aab

# ── 其他 ──
*.mp3
!public/*.mp3
!public/**/*.mp3
!public/card/
!public/card/**
*.log
.DS_Store
Thumbs.db

# ── 头像默认备份（换头像时自动生成，每人不同） ──
public/UserIcon.default.png
public/AIIcon.default.png

# ── 资源压缩备份（一次性压缩前的原始大图，本地安全网，不入库） ──
public/_orig_backup/

# ── 本地安装包 / 安装器不入库 ──
*.exe
tailscale-setup.exe

# ── 启动日志（运行时生成，量大） ──
start.log
start_err.log

# ── 散落临时调试/生成脚本与大二进制（不入库） ──
aion-chat/_*.py
aion-chat/batch_*.py
aion-chat/upload_to_netease_cloud.py
_template_extracted.html
/*.zip
/温叙远*.html
/ChatGPT*.png

# ── Agent 工作目录（settings.local.json 与 memory 里含 api_key/step_key，绝不入库） ──
.claude/
.workbuddy/

# ── 设计稿草图与素材大二进制（十几 MB，本地留着参考就行） ──
.tmp_figma/
resources/background_clothing_conf/

# ── 网易云歌单的专辑封面素材（13.5MB 书封 PNG，仅上传用，不入库） ──
专辑封面/
*.lnk

# ── 本机工具状态 / 会话数据 ──
.agents/
.antigravitycli/
.codex/
.superpowers/
.worktrees/

# ── 环境变量、密钥与签名文件 ──
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# ── 运行时生成的私人内容 ──
aion-chat/data/model_raw_responses/
aion-chat/data/english_corner_audio/
aion-chat/data/cli_debug/
aion-chat/data/*.db
aion-chat/data/*.sqlite
aion-chat/data/*.sqlite3
aion-chat/data/*.log

# data/ 默认全部是本机运行数据；只保留空目录占位
aion-chat/data/*
!aion-chat/data/chats/
!aion-chat/data/chats/.gitkeep
!aion-chat/data/monitor_logs/
!aion-chat/data/monitor_logs/.gitkeep
!aion-chat/data/activity_logs/
!aion-chat/data/activity_logs/.gitkeep
!aion-chat/data/screenshots/
!aion-chat/data/screenshots/.gitkeep
!aion-chat/data/uploads/
!aion-chat/data/uploads/.gitkeep
!aion-chat/data/tts_cache/
!aion-chat/data/tts_cache/.gitkeep

# ── 浏览器资料（含历史、Cookies、登录会话） ──
Connor-BrowserTool/browser-profile/
Connor-BrowserTool/node_modules/

# ── 本地 QA、截图与临时产物 ──
codex_art/
cache/
temp_wheel/
temp_*.png
temp_*.xml
androguard.db

# ── 非运行必需的私人设计资料 / 需求截图 ──
界面美化/
陪伴阅读功能需求/
surprises/

# Local backup plans contain machine-specific drive and archive locations
docs/superpowers/plans/2026-07-29-seven-day-data-backup.md
docs/superpowers/specs/2026-07-29-seven-day-data-backup-design.md
